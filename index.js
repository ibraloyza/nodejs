const {generateRandomNumber,celciusToFahrenheit} = require('./utils')



console.log(`the random number is ${generateRandomNumber()}`);
console.log(`celcius to fahrenheit is ${celciusToFahrenheit(0)}`);
